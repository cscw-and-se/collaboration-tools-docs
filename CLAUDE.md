# Claude Code 指南

本仓库的通用 Agent 规则在 `AGENTS.md`。Claude Code 进入本仓库后，请先阅读并遵守 `AGENTS.md`，再根据下面的补充规则工作。

## 协作目标

用户希望以后可以用口述方式让 Agent 直接生成、归档并接入文档站。因此不要只给建议；在用户明确要你写文档时，应该完成这几件事：

1. 把口述内容整理成适合阅读的 Markdown。
2. 判断应该放入哪个目录。
3. 创建或更新对应文档。
4. 必要时更新 `_sidebar.md`。
5. 如果内容依赖源码，先查源码再写。

## Claude 的工作方式

- 默认中文回复和中文写作。
- 先给一个很短的判断：这次内容应该放哪里，以及为什么。
- 如果归属明确，直接编辑；不要反复追问。
- 如果一个主题同时涉及 AI 和项目实现，优先判断读者真正要找什么：
  - 讲 AI 工具、Agent 使用方法、AI 工作流：放 `ai_coding/`。
  - 讲协同工具项目源码、架构、模块实现：放 `collaboration_tools/`。
  - 讲“如何让 Agent 维护本文档”：优先更新根目录 `AGENTS.md` / `CLAUDE.md`。
- 新建面向读者的页面后，同步更新 `_sidebar.md`。
- 内部协作说明文件不必放进 `_sidebar.md`，除非用户希望读者也能看到。

## 质量底线

- 不要生成没有来源的项目实现细节。
- 不要把多个主题硬塞进一个长文档；该拆就拆。
- 不要创建重复页面；先搜索相近主题。
- 不要为了显得完整而写大段泛泛背景。
- 不要修改 `collaboration-tools/` 源码，除非用户明确要求。

## 常用检查命令

```bash
rg "关键词" .
rg --files
docsify serve
```

如果只改 Markdown，通常不需要跑项目测试；但要检查链接、目录、标题和侧边栏。
