# Agent 写作指南

这是一份给 Coding Agent / 文档 Agent 使用的仓库说明。以后用户用口述方式让你补充文档时，请先读这份文件，再决定新内容放在哪里、是否需要更新导航、是否需要查源代码或资料。

## 这个仓库是什么

本仓库是 `collaboration-tools-docs`，使用 docsify 组织 Markdown 文档，目标是沉淀实时协同编程、协同工具项目、AI 编程、Git、VS Code 插件开发和常用工程知识。

读者主要是刚加入项目的学生、参与实时协同工具开发的人，以及想快速上手相关技术栈的人。写作时优先服务“新人能读懂并照着做”，不要写成只给作者自己看的备忘录。

## 基本工作流

当用户口述一个新主题或修改需求时，按这个顺序处理：

1. 判断内容归属，选择合适目录。
2. 检查是否已有相近文档，优先补充已有页面，避免重复开新坑。
3. 如果是新页面，创建对应 `.md` 文件，并同步更新 `_sidebar.md`。
4. 如果涉及截图或图片，把资源放在当前文档同级的 `assets/` 目录中，使用相对路径引用。
5. 如果内容依赖项目代码，先查 `collaboration-tools/` 或相关源码，再写文档，不要只凭印象生成。
6. 完成后快速检查链接、标题层级、目录归属和 docsify 可读性。

## 内容应该放哪里

### AI 编程相关：`ai_coding/`

放这里的内容包括：

- AI 编程工具、Coding Agent、Claude Code、Codex、Cursor、Gemini CLI 等。
- 使用 AI 读代码、写代码、做项目、做学习路线。
- Agent、MCP、工具调用、Memory、Prompt、AI Harness 等 AI 协作方法论。

如果新内容是“怎么用 AI 写这个文档”“怎么让 Agent 接管某个流程”，也优先放在 `ai_coding/`，除非它明显是在说明本仓库协作规则，那就放根目录的 `AGENTS.md` / `CLAUDE.md`。

### 实时协同编程理论：`rcp/`

放这里的内容包括：

- RCP / Real-Time Collaborative Programming 的概念解释。
- OT、CRDT、协同编辑算法和相关研究方向。
- 偏理论、偏综述、偏概念辨析的内容。

### 技术栈入门：`tech_stack/`

放这里的内容包括：

- Shell、TypeScript、Y.js、前后端开发等基础技术。
- 面向新人补课的前置知识。
- 不直接绑定本项目某个模块，但对理解项目有帮助的技术背景。

### Git 协作：`git/`

放这里的内容包括：

- Git 基础操作。
- 分支、PR、冲突解决、团队协作流程。
- 面向团队实践的 Git 工作规范。

### VS Code 插件开发：`vscode_plugin/`

放这里的内容包括：

- VS Code Extension API。
- 命令、UI、Webview、编辑器、文件系统、配置、状态管理。
- 插件开发教程和实践说明。

### 协同工具项目文档：`collaboration_tools/`

放这里的内容包括：

- `collaboration-tools/` 源码项目的架构、模块、协议、认证、二次开发。
- open-collaboration-server / protocol / vscode / yjs / monaco / agent 等模块讲解。
- 从源码反推出来的实现机制、调用链、数据流和二次开发指南。

如果用户说的是“这个项目里某个模块怎么运行”“帮我解释这段源码”“把源码整理成文档”，通常应该写到这里。

写这类文档前必须先读相关源码。不要把通用概念文档伪装成项目实现说明。

### 课程笔记：`courses/`

放这里的内容包括：

- CMU 17-803、Missing Semester 等课程的整理笔记。
- 课程内容总结、学习路线、作业或阅读材料提炼。

### 零散笔记：`random_notes/`

放这里的内容包括：

- 不属于上面专题，但仍然值得沉淀的工具使用、写作方法、资料整理。
- EndNote、文献综述、公众号推荐、文档编辑技巧等。

如果一个主题未来可能扩展成独立专题，不要急着放 `random_notes/`；先判断是否应该开一个更明确的顶层目录。

### 站点入口与导航：根目录

根目录文件一般只在这些情况下修改：

- `README.md`：文档站首页和项目说明。
- `_sidebar.md`：docsify 侧边栏导航，新建页面后通常要同步更新。
- `index.html`：docsify 配置和页面入口，除非改站点行为，否则不要动。
- `AGENTS.md` / `CLAUDE.md`：给 AI 协作者看的仓库规则，不进入普通读者导航也可以。

## 写作风格

- 默认使用中文。
- 面向新人解释，先讲“它解决什么问题”，再讲“怎么做”，最后补“为什么这样做”。
- 保持口吻自然，不要写成正式公文，也不要写成营销文案。
- 技术文档要有具体命令、路径、文件名、代码入口或检查方式。
- 避免空泛标题，比如“深入理解”“全面解析”。标题要让读者知道这一节到底讲什么。
- 可以使用表格，但不要为了显得完整而堆表格。
- 代码块要标注语言，例如 `bash`、`ts`、`json`、`md`。
- 对不确定的信息要说明来源或标记为待验证，不要编造。

## 新建文档模板

新建专题页时优先使用这个结构，按需删减：

```md
# 标题

用一两段说明这篇文档要解决什么问题，以及读者读完能做什么。

## 适合谁读

- 读者类型 A
- 读者类型 B

## 核心概念

解释必要背景，尽量用项目里的实际例子。

## 操作步骤 / 实现机制

给出具体步骤、调用链、代码入口或命令。

## 常见问题

记录容易踩坑的地方和排查方式。

## 延伸阅读

列出仓库内相关文档或外部资料。
```

如果只是补充已有页面，不需要强行套模板，保持原文风格即可。

## 更新 `_sidebar.md` 的规则

新建对普通读者有价值的页面时，必须更新 `_sidebar.md`。

- AI 相关页面挂到“什么是AI编程”。
- RCP 理论挂到“实时协同编程”或“深入实时协同编程”。
- 技术基础挂到“技术栈”。
- Git 内容挂到“Git”。
- VS Code 插件内容挂到“VS Code插件开发”。
- 项目实现文档挂到“项目概览”或“项目分模块讲解”。
- 零散工具/写作方法挂到“一些笔记”。

如果只是给 Agent 使用的内部说明，例如 `AGENTS.md`、`CLAUDE.md`，可以不加入 `_sidebar.md`。

## 写项目实现文档时的最低要求

涉及 `collaboration-tools/` 源码时，必须做到：

- 标出相关源码路径，例如 `collaboration-tools/packages/...`。
- 区分“源码里已经实现的行为”和“可以这样扩展的建议”。
- 如果解释调用链，至少读入口文件、核心类和相关类型定义。
- 不要只根据文件名猜功能。
- 不要重命名、移动或改源码，除非用户明确要求改代码。

## 修改边界

- 文档任务默认只改 Markdown、图片资源和 `_sidebar.md`。
- 不要改 `collaboration-tools/` 源码，除非用户明确要求。
- 不要删除已有文档，除非确认它已经被合并或替代。
- 遇到已有未提交改动时，先看是否相关；不相关就不要碰。
- 如果用户只是要“给我指令”，不要直接改文件。

## 完成前检查

提交结果前至少检查：

- 新页面是否在正确目录。
- `_sidebar.md` 是否需要更新，且链接路径正确。
- Markdown 标题层级是否从一个 `#` 开始。
- 图片路径是否能从当前 Markdown 文件解析。
- 是否留下了“TODO”“待补充”但没有解释原因。
- 是否把 AI 生成的空话写进了正文。

可选本地预览命令：

```bash
docsify serve
```

浏览器打开 `http://localhost:3000` 检查页面。
